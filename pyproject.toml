[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "developing-the-researcher"
version = "0.1.0"
description = "Personalized adversarial committees for competency development against mechanical AI use"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.25.0",
    "beautifulsoup4>=4.12.0",
    "sentence-transformers>=2.2.0",
    "transformers>=4.36.0",
    "accelerate>=0.25.0",
    "bitsandbytes>=0.41.0",
    "numpy>=1.24.0",
    "scikit-learn>=1.3.0",
    "matplotlib>=3.7.0",
    "seaborn>=0.13.0",
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]
presentations = ["mcp-pptx-tools>=1.0.0"]

[tool.setuptools.packages.find]
where = ["src"]
